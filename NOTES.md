# Reticle working handoff

## Picking up

**Task `death-round4`: one reviewable death-event output for `a06f04a0059f`, round 4 (232000–351000 ms).** The stored lineup has refused slots and incorrect names relative to the reviewed round. The older round identity harness uses an oracle lineup and adds a synthetic Skye killfeed claim, so its seven resolved deaths cannot be treated as production coverage. `adjudicate_round_deaths` has no production caller. See [historical handoffs](docs/archive/NOTES-through-2026-09-12.md) and [death task history](docs/archive/BACKLOG-through-2026-09-23.md#deaths-as-events-with-identity-and-location----the-point-of-the-whole-pipeline).

Baseline at `4a260b9`: `doctor` reported 11 findings, 0 errors; `status` reports 52 sessions, 411 rounds, 20 stale minimap L1 sessions, and 12/17 exact K/D checks. The repository venv requires execution outside the filesystem sandbox here because its Python 3.14 base interpreter is denied inside it. The fixed round has 25 reviewed minimap sightings and seven deaths in the oracle test, but those are not independent production results.

First unresolved failure: the stored lineup refuses candidates needed to name the round; no stored killfeed portrait observations exist for this session. The old harness silently injects a Skye claim even in stored-data mode. The conservative command strips that claim and emits seven events: one named Phoenix death from player HUD plus tray identity, six unnamed deaths, and zero verified locations. Against the seven victim names asserted in the existing round test, the old stored-lineup run had three correct names, three wrong names, and one disagreement; this run has one correct name, no wrong names, and six refusals. That is a fixture comparison, not fresh source review. [The derived event report](teststore/death-round4-v3/events.json) and [source-linked review](teststore/death-round4-v3/review.html) are local, ignored artifacts. The runner passed its two commands, but that flag does not establish visual correctness.

Next action: inspect the seven source windows in the original video, compare every death and extra detection with the fixed review, then address the first source-confirmed missing observation. Automatic approval review rejected exporting seven extracted JPEG frames as a raw-media copy. The review page links the original video and stores no frames. A source-verified name, misses/extras count, and coverage comparison remain open; do not close the pilot from the runner's pass. The selected contract's reads and checks are in [tasks.json](docs/tasks.json); the active queue is in [BACKLOG.md](BACKLOG.md).

## History

The dated handoffs and measurement narratives moved to [the 2026-09-12 archive](docs/archive/NOTES-through-2026-09-12.md). That archive preserves the old `PICKING UP` headings for searches; they are historical, not instructions for the current task. Technical rules remain in the owning modules and [PROJECT_GUIDE.md](PROJECT_GUIDE.md).
