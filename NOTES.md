# Reticle working handoff

## Picking up

**Task `death-scoreboard-binding`: test an independent stored scoreboard/top-bar identity binding for `a06f04a0059f` round 4.** Read the [five-refusal diagnosis](docs/DEATH_ROUND4_REFUSALS.md) and its [contract](docs/tasks.json). The fixed round is 232000–351000 ms. Establish a known scoreboard row and identity baseline, inspect source, and log falsifiable predictions in the store before a perceptual experiment. Do not promote human review answers, lineup best guesses, or K/D arithmetic as a machine identity.

The completed `death-refusals` contract passed. All five raw portrait keys, lineup alternatives, and independent channel gaps are in the diagnosis. The player source-reviewed the five refused victims as Deadlock, Jett, Miks, Skye, and Iso; Omen and Breach best matches disagree with source. The real command still emits seven deaths, two correct machine names (Reyna and Phoenix), five identity refusals, zero verified locations, and no new extra or wrong named event in the reviewed set. The focused 25 tests pass; `doctor` had 11 findings and zero errors. The new review artifact is `teststore/death-round4-refusals/`; prior artifacts were preserved.

Task record: accepted; six required reading routes were selected and the relevant sections inspected. Measured machine correction: zero, by design; five refusal causes and five player answers were recorded without promotion. The seven-event real output and approved source frames were reviewed. Development time and token use were not measured.

The untracked `prototypes/mechanics_eval.py` belongs to the user and must remain untouched. Historical handoffs and measurements are in [the dated archive](docs/archive/NOTES-through-2026-09-12.md). The [backlog](BACKLOG.md) orders current work; the [working map](docs/WORKING_MAP.md) routes required subsystem reading.
