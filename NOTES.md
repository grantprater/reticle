# Reticle working handoff

## Picking up

**Combat report accepted on the corpus (2026-09-24).** All 20 sessions with HUD reads are scanned (`killfeed-portrait-0.3.0`, `combat-report-0.4.0`), and the per-round verdict equals `checks.KNOWN_KD` on all 17 that have one, e.g. [metric:combat_report/acceptance@043bafca271a#kills_verdict=11] and [metric:combat_report/acceptance@c62c2b06bcfb#deaths_verdict=15]. Recorded per session as `combat_report/acceptance` (`prototypes/combat_report.py acceptance`). Every report/killfeed disagreement was read in frames and all are killfeed errors: deaths it never saw, an ability kill, a kill of an enemy on a second life, a Me -> Me self entry, a split track, a post-round kill where rounds touch. Frames also fixed three report rules (`combat-report-round-0.8.0`): a team kill (KILLED on an ALLY row) is not a kill; the death flash can delay the killfeed 2.5 s past the panel; an early panel that does not read the previous round's frozen death panel is its own round's death. The player confirmed `5822b6646448` round 16: a Run It Back death then a real death by the same enemy, one report row summing both [domain:rounds/run-it-back-in-report]; the killfeed never marked the real one. ALLY rows are read by the word in the weapon slot (13 corpus-wide, none with a hit) and bounded by the ally lineup; none is named yet. Rows named: [metric:combat_report/acceptance@a06f04a0059f#rows_named=49] of 50 there, 539 of 721 corpus-wide with 16 identity disagreements, 13 of them involving the scoreboard bound-of-one channel -- read those next. `reticle deaths` stores death verdicts keyed by entry onset and slot, and report rows bind to them (every KILLED YOU row on the two labelled sessions). `status` shows the verdict through `round_verdicts`; `coach` (0.2.0) attaches it and counts Run It Back as `player_second_life`. Weapons: the player's magazine | reserve table is [domain:weapons/magazine-and-reserve] (three pairs shared); the HUD ammo counter reads at 304 of 307 kills and is the only stored witness of the player's gun. Decomposition [domain:combat_report/damage-decomposition] stays in the prototype until a capacity reader exists.

**Weapons named from the killfeed; revives are not deaths (2026-09-24).** `reticle scan` stores each killfeed entry's weapon-slot descriptor (`killfeed_weapon`); `reticle deaths` names it through `adjudication.weapon` and `weapon-gallery-0.2.0`, built by `prototypes/weapon_icons.py gallery` from the player's group names (`labels/weapon_icon/`) and per-entry names (`labels/killfeed_icon/`): a gallery of the player's names. 0.2.0 splits the "Ability" group into Aftershock, Orbital Strike, Boom Bot, Not Dead Yet and Resurrection; left one session out it named [metric:weapon_icons/entries-loso@corpus#not_dead_yet_right=12] of 16 Not Dead Yet entries, none wrongly. `death-adjudication-0.9.0` stores an entry whose icon is Not Dead Yet or Resurrection as a revive (`is_revive`) [domain:killfeed/revive-entries]: 22 corpus-wide, two unlabelled ones confirmed in frames, and the scoreboard witness refuses an interval that holds a revive (a revived Skye died again at `b3b9defb6fd7` 1308.0 s). The combat report equals `KNOWN_KD` on all 8 revive sessions. **The weapon-slot locator (hud-0.13.0, killfeed-weapon-0.2.0)** prefers line art (median V 250+, S 20-) so pale plates stop outscoring ringed ult icons, admits the Blade Storm knife [domain:killfeed/jett-blade-storm-icon] as a last tier, and ignores the killstreak numeral [domain:killfeed/killstreak-indicator] as a name. From the crop cache over 20 sessions: [metric:weapon_slot_locator/corpus-cache@corpus#box_moved=431] boxes moved, none lost, three verdicts changed, all corrections. After the rescan the K/D is unchanged on 20 and the report equals `KNOWN_KD` on 17; 32 identity verdicts moved with the portrait crops, none to another name. The player then labelled the knives: `weapon-gallery-0.3.0` names Blade Storm (17 of 17 left out by session), weapons resolved [metric:weapon_reader/accept@corpus#weapon_resolved=2894], revives 34, and the hexagon on a self entry is an environmental death [domain:killfeed/environmental-self-entry]. Next: ability icons as killer identity claims, both ways: an icon narrows the agent, a named agent narrows the icon.

**Entity mining toward production (2026-09-24).** `prototypes/entity_mining_residual.py` seeds area abilities from `lighting.raw_dark` births after tray casts, with no hand coordinates, and reports censored lifetimes. On four solo demos it recovers Dark Cover's 15 s in [metric:entity_mining_residual/solo-demo#dark_cover_unique_contains_15=3] of 3 uniquely assigned uses; two casts 1.5 s apart claimed one smoke, so cast-entity assignment must be joint. `minimap.detect_ability_discs` is the wrong seed for smokes: Ascent's static dots flicker past `static_peaks`. Player-confirmed since: [domain:abilities/jett-cloudburst-duration], [domain:abilities/viper-poison-cloud-toggle] (three toggles at one spot on `afa5bc60b935`), which fits [domain:abilities/viper-poison-cloud-fixed], [domain:abilities/viper-pit-tint]. Open defects: the settings menu's dim overlay passes `minimap.widget_drawn` (seen on `6bb88dba5d2c` at 44.0 s); `radar_circular_mask` centres an ROI-inscribed circle, but the widget ring circumscribes the map [domain:minimap/widget-ring] and the mask clips map pixels on 6 of 12 geometries -- fix it from a stable ring fit, not the ROI. The ally reader's ring surface is now computed per blob (identical output, feed 1.66x faster on a busy window); decode prefetch measured 2-9% and was dropped. **Smokes are in production:** `reticle scan --only minimap_dark` stores grey-dark and occluded masks at 4 Hz (`minimap_dark`, the full match in 301 s), and `reticle smokes` recomputes tracks with censored onsets and ends from storage. On `a06f04a0059f` [metric:smokes/match@a06f04a0059f#both_ends_observed=22] tracks have both ends observed and all read 17.75-18.0 s, the Miks disc [domain:abilities/miks-smoke-minimap-disc]; Miks is the team's only smoke agent, so the lone-agent rule [domain:abilities/smoke-attribution] names them all. Miks often places two smokes at the same instant. Next: attribution in `adjudication.smokes`, then a second session with two smoke agents, then placement and timing against ally movement.

**Least-work testing (2026-09-25).** `reticle plan` lists stale streams and the least work that refreshes them; `reticle trial --reader killfeed|hud` reruns a reader on stored frames and diffs it against storage, writing nothing. `scan --cache-roi hud` stores lossless crops of the HUD regions (score line with the spike, health, ammo, killfeed, team bars, centre; 19 GB for 20 sessions; crops are not a media copy, player 2026-09-25). On `a06f04a0059f` a killfeed recheck took [metric:scan_tiers/killfeed-recheck@a06f04a0059f#current_s=184.0] s as a scan and [metric:scan_tiers/killfeed-recheck@a06f04a0059f#tier3_s=17.8] s from the cache; a whole-session HUD trial from cache reproduces its 3811 rows in 81.5 s. The plan found the minimap tables at 0.1.0-0.5.0 on 19 sessions against `minimap-0.7.0`. Next: a minimap crop set at 15 Hz. Clove's expiry is a death [domain:rounds/clove-revive-expiry-entry].


The untracked `prototypes/mechanics_eval.py` belongs to the user and must remain untouched. Historical handoffs are in the dated archives ([to 09-12](docs/archive/NOTES-through-2026-09-12.md), [to 09-24](docs/archive/NOTES-through-2026-09-24.md)). The [backlog](BACKLOG.md) orders current work; the [working map](docs/WORKING_MAP.md) routes required subsystem reading.
