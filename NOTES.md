# Reticle working handoff

## Picking up

**Task `death-portraits`: persist real killfeed portrait observations for `a06f04a0059f` round 4 and resolve at least one additional victim.** The fixed round is 232000–351000 ms. Keep raw portraits separate from identity adjudication, retain refused lineup rivals, and use the shared HUD decode pass. The [contract](docs/tasks.json) requires two source-verified named victims without a new wrong name.

The `death-round4` pilot is reviewed and complete. Its [event report](teststore/death-round4-v3/events.json), seven approved [annotated frames](teststore/death-round4-v3/), and [review summary](teststore/death-round4-v3/review-summary.md) account for seven visible killfeed deaths, four ally and three enemy roster shrinks, zero misses or extras in the fixed reviewed set, one source-verified Phoenix identity, six explicit identity refusals, and zero verified locations. This is one round's result. The repeatable contract passes; `doctor` reports 11 findings and zero errors. The historical stored-lineup harness named six victims, three incorrectly against the fixed review assertions, and injected a synthetic Skye claim.

**First failure:** this session has no stored killfeed portrait observations. The stored lineup also refuses slots needed for the other names. The next action is to persist versioned portrait observations on the shared HUD pass, then use them as evidence in death adjudication. Inspect the first newly promoted name in the source review and stop if it disagrees. The first refused rival cannot be turned into a name from the lineup's `best_guess` alone.

Starting state: commit `632b3ef` plus the reviewed pilot update. The untracked `prototypes/mechanics_eval.py` belongs to the user and must remain untouched. Historical handoffs and measurements are in [the dated archive](docs/archive/NOTES-through-2026-09-12.md). The [backlog](BACKLOG.md) orders current work; the [working map](docs/WORKING_MAP.md) routes required subsystem reading.