# Reticle working handoff

## Picking up

**Killfeed attribution (2026-09-25).** `reticle deaths` names each entry's weapon (`weapon-gallery-0.3.0`), stores revives, publishes an ability icon's caster as a killer claim, and gives every name `p_named` from `adjudication.reliability`; the history is in the [09-25 archive](docs/archive/NOTES-through-2026-09-25.md). **Killer crops start at the name.** `_band_text` keeps only glyphs on the baseline, so a descender (the y of "Reyna") split the killer's name and the crop sat on it with the face to its left; `killfeed.killer_name_start` (`killfeed-portrait-0.5.0`, measured by `prototypes/killer_portrait_anchor.py`) walks left over glyphs that reach the baseline. [metric:killer_portrait_anchor/corpus-cache@corpus#anchors_moved=6663] of 30244 killer crops moved; resolved killers rose to [metric:killer_portrait_anchor/corpus-cache@corpus#killers_resolved_after=2008] from 1789; all 13 swapped names read as corrections in frames. The player's 120 portrait labels (`prototypes/label_feed_portraits.py`) still score 116 right, 2 wrong, but they sample only names the old crops resolved: the gained names need a new uniform sample. Three new witness disagreements: `a1a995e6b19b` 553.5 s (player_hud bound the player's kill to a second entry; the portrait is right), `b7d24102a6f6` 858.0 s (a Clove expiry read as Reyna), `bfad2778a372` 1942.5 s (a victim clipped at the ROI edge). **Assist panel** [domain:killfeed/assist-panel]: layout measured [domain:killfeed/assist-panel-layout]; an icon-less assister is likely a damage assist [domain:killfeed/assist-without-icon]; yellow marks the player [domain:killfeed/self-yellow-frame]. `prototypes/label_assists.py label` asks the player for the count, agents and icons on 140 prepared entries (93 of 850 that `prototypes/assist_panel.py` edge-flags, 47 of 1838 it does not). Next: the player labels; then a panel reader scored on those labels, seeded from the per-agent reliability table.

**Entity mining toward production (2026-09-24).** `prototypes/entity_mining_residual.py` seeds area abilities from `lighting.raw_dark` births after tray casts, with no hand coordinates, and reports censored lifetimes. On four solo demos it recovers Dark Cover's 15 s in [metric:entity_mining_residual/solo-demo#dark_cover_unique_contains_15=3] of 3 uniquely assigned uses; two casts 1.5 s apart claimed one smoke, so cast-entity assignment must be joint. `minimap.detect_ability_discs` is the wrong seed for smokes: Ascent's static dots flicker past `static_peaks`. Player-confirmed since: [domain:abilities/jett-cloudburst-duration], [domain:abilities/viper-poison-cloud-toggle] (three toggles at one spot on `afa5bc60b935`), which fits [domain:abilities/viper-poison-cloud-fixed], [domain:abilities/viper-pit-tint]. Open defects: the settings menu's dim overlay passes `minimap.widget_drawn` (seen on `6bb88dba5d2c` at 44.0 s); `radar_circular_mask` centres an ROI-inscribed circle, but the widget ring circumscribes the map [domain:minimap/widget-ring] and the mask clips map pixels on 6 of 12 geometries -- fix it from a stable ring fit, not the ROI. The ally reader's ring surface is now computed per blob (identical output, feed 1.66x faster on a busy window); decode prefetch measured 2-9% and was dropped. **Smokes are in production:** `reticle scan --only minimap_dark` stores grey-dark and occluded masks at 4 Hz (`minimap_dark`, the full match in 301 s), and `reticle smokes` recomputes tracks with censored onsets and ends from storage. On `a06f04a0059f` [metric:smokes/match@a06f04a0059f#both_ends_observed=22] tracks have both ends observed and all read 17.75-18.0 s, the Miks disc [domain:abilities/miks-smoke-minimap-disc]; Miks is the team's only smoke agent, so the lone-agent rule [domain:abilities/smoke-attribution] names them all. Miks often places two smokes at the same instant. Next: attribution in `adjudication.smokes`, then a second session with two smoke agents, then placement and timing against ally movement.

**Least-work testing (2026-09-25).** `reticle plan` lists stale streams and the least work that refreshes them; `reticle trial --reader killfeed|hud` reruns a reader on stored frames and diffs it against storage, writing nothing. `scan --cache-roi hud` stores lossless crops of the HUD regions (score line with the spike, health, ammo, killfeed, team bars, centre; 19 GB for 20 sessions; crops are not a media copy, player 2026-09-25). On `a06f04a0059f` a killfeed recheck took [metric:scan_tiers/killfeed-recheck@a06f04a0059f#current_s=184.0] s as a scan and [metric:scan_tiers/killfeed-recheck@a06f04a0059f#tier3_s=17.8] s from the cache; a whole-session HUD trial from cache reproduces its 3811 rows in 81.5 s. The plan found the minimap tables at 0.1.0-0.5.0 on 19 sessions against `minimap-0.7.0`. `scan` now feeds a pass from the cache when every reader in it reads only cached ROIs (`--from auto`, the default; `--from video` decodes): a whole-session killfeed-portrait rescan took [metric:scan_from_cache/killfeed-portrait@043bafca271a#cache_scan_s=135.9] s against about 30 min decoded, rows identical. Open: the ally roster ROI ends at px 751 and cuts the ally portrait nearest the scoreline; the player saw probe images without the enemy roster, though the `hud` cache stores `hud_roster_enemy`. Next: a minimap crop set at 15 Hz. Clove's expiry is a death [domain:rounds/clove-revive-expiry-entry].


The untracked `prototypes/mechanics_eval.py` belongs to the user and must remain untouched. Historical handoffs are in the dated archives ([to 09-12](docs/archive/NOTES-through-2026-09-12.md), [to 09-24](docs/archive/NOTES-through-2026-09-24.md), [to 09-25](docs/archive/NOTES-through-2026-09-25.md)). The [backlog](BACKLOG.md) orders current work; the [working map](docs/WORKING_MAP.md) routes required subsystem reading.
