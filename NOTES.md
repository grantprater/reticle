# Reticle working handoff

## Picking up

**Ally minimap identity, first slice (2026-09-23).** Step 6 in the [backlog](BACKLOG.md) is under way. `minimap.AllyIconReader` rides `reticle scan` as the `ally_icon` channel and writes `ally_icon` events at `ally-icon-0.1.0`; minimap positions stay at `minimap-0.7.0`. `identity.claims_from_ally_icons` names each frame's icons over the board's ally set minus the player. `tools/ally_identity_review.py` measures a session and renders a source sample. On `a06f04a0059f` it names 63% of described icons. The prediction failed on its error clause: 2 of 40 sampled names are wrong, and both are overlapping icons whose ring fit covers two portraits ([results](docs/ALLY_MINIMAP_IDENTITY.md)). The predictions log records the outcome. Next is a player-labelled sample before any overlap gate; do not tune against the 40, which selected the failure. Only `a06f04a0059f` has `ally_icon` events.

**Earlier today.** Scoreboard lineups hold across the corpus ([results](docs/SCOREBOARD_LINEUP.md#corpus-at-scoreboard-050)); board alive sets, killers and the session-exemplar loop are done ([backlog](BACKLOG.md) Completed). Ability detection and appearance are wired under `[owns:ability-detection]` and `[owns:ability-appearance]`, status partial. `tools/identity_loop.py`, the round-4 review and `tools/ally_identity_review.py` refuse to overwrite their output directories; pick a new path. The e2e tests read gitignored `fixtures/`, so a fresh git worktree needs them linked (a junction), not copied, and removed with `rmdir` before the worktree.

The untracked `prototypes/mechanics_eval.py` belongs to the user and must remain untouched. Historical handoffs and measurements are in [the dated archive](docs/archive/NOTES-through-2026-09-12.md). The [backlog](BACKLOG.md) orders current work; the [working map](docs/WORKING_MAP.md) routes required subsystem reading.
