# Reticle working handoff

## Picking up

**Task `death-scoreboard-binding` is blocked at its first machine row binding.** Read the [attempt and player follow-up](docs/DEATH_SCOREBOARD_BINDING.md), the [five-refusal diagnosis](docs/DEATH_ROUND4_REFUSALS.md), and the [contract](docs/tasks.json). The player identified the second scoreboard row and observed that it is dimmed, while remaining unsure whether dimming always means dead. Source openings support the dimming hypothesis for this interval, but the stored cross-surface portrait comparison still ranks an enemy row first and has no validated binding or row name. The player answer stays review truth. Build a versioned name or agent witness and a checkable death binding before promoting another identity.

Ordering follow-up: the source shows WhaleKicker and Me swap rows after Me gains a kill. The inspected August openings are consistent with kills and assists affecting rank, but no exact sort rule or deaths tie-break was established, and only 4/80 stored round rows have complete K/D/A. `display_row` is not a player key. The ordering evidence and current game-rule change are in the diagnosis.

The real round still emits seven deaths, two correct machine names, five identity refusals, and seven location refusals. The three-name gate fails; the focused 25 tests pass. All seven approved source composites were reviewed. Task record: blocked; seven required reading routes inspected; measured machine correction zero; output reviewed; development time and token use unknown. Pickup `doctor` had 11 findings and zero errors. The new artifact is `teststore/death-round4-scoreboard/`; prior artifacts were preserved.

The untracked `prototypes/mechanics_eval.py` belongs to the user and must remain untouched. Historical handoffs and measurements are in [the dated archive](docs/archive/NOTES-through-2026-09-12.md). The [backlog](BACKLOG.md) orders current work; the [working map](docs/WORKING_MAP.md) routes required subsystem reading.
