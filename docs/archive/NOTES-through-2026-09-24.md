# NOTES archive through 2026-09-24

Paragraphs retired from `NOTES.md` on 2026-09-24, verbatim.

**Combat report in production (2026-09-24).** `reticle scan --only combat_report` reads the panel [domain:combat_report/panel-layout] at 1 Hz with no labels; `reticle combat-report` assigns panels to rounds (deaths, and the buy-phase summary [domain:combat_report/round-summary]) and stores a per-round verdict beside the killfeed's count. The verdict gives the known K/D on both sessions scanned, [metric:combat_report/flags@a06f04a0059f#kills=19] and [metric:combat_report/flags@3694746e4e54#deaths=17]. It fixed `rounds`: round-ending and post-round events [domain:rounds/post-round-period], the unread final round [domain:rounds/match-end]. Run It Back deaths are now read by their badge and kept out of `player_deaths` (`round-0.6.0`); the two disagreements left are killfeed entries split into two tracks. Rows are named through `adjudication.identity` from enemy killfeed portraits within the scoreboard K/D bound, by portrait cluster: [metric:combat_report/identity@a06f04a0059f#right=49] of 50 player labels, 34 of 37 on `3694746e4e54`, none wrong. Split killfeed tracks are merged (`round-0.7.0`); both sessions now agree with the report on every round. Damage decomposition [domain:combat_report/damage-decomposition] stays in the prototype. Next: corpus acceptance (backlog).

**Statistical machinery and directional entity mining in production (2026-09-24).** Production incorporates the validated statistical optimization models:
1. Killfeed spatial alignment search under crop uncertainty (`killfeed.py`, `adjudication/identity.py`): anchors killer portraits to killer names and searches displacement $\Delta x \in [-4, +4]$ px with quadratic penalty ($\lambda = 0.0015$). On `a06f04a0059f` Round 4 at 284500 ms (victim Jett), killer Breach passes across all 4 entry views (margin 0.104--0.146, 100% up from 25%).
2. Wall-edge relaxation & dark object gating (`lighting.py`, `cli.py`, `adjudication/ability.py`): `lighting.raw_dark` stores floor drops $\ge 15.0$ below `ref.lo` (`ability-light-0.2.0`). `light_refusals` combines 1-px wall relaxation with raycasts ($\pm 2$ px, $\pm 10^\circ$), refusing 19 of 19 viewcone fragments (100.0%) and 0 of 34 abilities (0.0%) on grouping labels (`ability-light-refusal-0.2.0`).
3. Parametric trajectory schema & radar circular masking (`minimap.py`, `adjudication/ability.py`): `radar_circular_mask` confines disc and wall detection to the circular radar boundary, preventing perimeter step-edge artifacts. Directional abilities (`sova:hunter's fury`, `breach:rolling thunder`) emit `trajectory_schema` `beam(x0, y0, theta, L, w)`. All 671 tests pass; `doctor` reports 0 errors.

**Domain-hypothesis foundation checkpoint (2026-09-23).** `reticle domain-hypothesis` imports pinned evidence proposals and publishes review bundles. Synthetic fixture `tests/data/domain_learning_synthetic.json` demonstrates transitive feedback, retained contradiction, fact proposals, and withdrawal dry runs (`teststore/domain-learning-synthetic/current.json`). Next integrate real stored observations and artifact revision resolving before fact promotion.

**Earlier today.** Scoreboard lineups hold across corpus ([results](docs/SCOREBOARD_LINEUP.md#corpus-at-scoreboard-050)); board alive sets, killers and exemplar loop are done ([backlog](BACKLOG.md)). Ability detection and appearance are wired under `[owns:ability-detection]` and `[owns:ability-appearance]`. Fresh worktrees need `fixtures/` linked, not copied, removed with `rmdir`.

## Retired from NOTES.md on 2026-09-24

**Minimap candidate contract (2026-09-23).** `AllyIconReader` stores fitted hypotheses under content-addressed candidate revisions; `adjudication.minimap_candidates` (`[owns:minimap-icon-disposition]`) decides each one; `ally_icon` events replay the accepted view (`ally-icon-0.3.0`, [contract](docs/MINIMAP_CANDIDATE_CONTRACT.md)). 15 Hz rescan of `a06f04a0059f` reproduces all [metric:ally_candidate_replay/15hz@a06f04a0059f#icons=75643] 0.2.0 rows, changing [metric:ally_candidate_replay/15hz@a06f04a0059f#icons_changed=0], keeping [metric:ally_candidate_replay/15hz@a06f04a0059f#ally_not_selected=42087] dropped fits (`analysis/ally_icon_events_020_15hz_a06f04a0059f.jsonl`). **To finish lobe fix:** `reticle lifetimes a06f04a0059f` and test prediction: $\le 450$ ally entities, refits down $\ge 40\%$ from 519.

**Round lifetimes wired (2026-09-23).** `reticle lifetimes SESSION` runs `round_lifetimes` over stored ally icons, rounds and roster, writing `round_entity` events; `a06f04a0059f` has 15 Hz events. Adjacent births were fits jumping onto teardrop lobes; `track.refit_of` owns resolution limits and `RoundLifetimes` calls it: ally entities 823 -> 576. Next fix lobe-centred fit, missed icons, merged state, and start/end events ([results](docs/ROUND_ENTITIES.md)).

**Ally minimap identity, first slice (2026-09-23).** Step 6 in [backlog](BACKLOG.md). `minimap.AllyIconReader` runs as `ally_icon` channel (`ally-icon-0.1.0`); minimap positions stay `minimap-0.7.0`. `identity.claims_from_ally_icons` names icons over ally set minus player. On `a06f04a0059f` it names 63% of described icons; 2 of 40 sampled names failed where overlapping icons cover two portraits ([results](docs/ALLY_MINIMAP_IDENTITY.md)). Next is player-labelled sampling before an overlap gate.

**Audio identity channel, first loop (2026-09-24).** `prototypes/audio_channel.py` treats audio as an identity channel beside the tray and ammo counter hidden during non-gun holds [domain:abilities/ammo-hidden-while-non-gun-held]. Within a session a 0.6 s log-mel template finds its partner first in [metric:audio_channel/retrieve#corr_hits=15] of 16 queries on Omen `b9558488a607` and Deadlock `29eff6920e8f`, and named a cancelled Paranoia equip; video agreed. Voice lines mask some uses [domain:abilities/ability-voice-line-mask]; a one-sided score recovers the masked Omen ult equip. Across sessions (`e78e75b2d191`) C, Q and X transfer, Dark Cover does not [metric:audio_channel/transfer@e78e75b2d191#E_top_in_slot=0], and E-equip matches the knife above true equip. Next build a reject class (knife, gun, footsteps) and multi-session references before setting thresholds. Deadlock tray X drop is ult equip [domain:abilities/deadlock-ult-tray-drop-at-equip]; GravNet flash is a minimap yellow disc under 0.3 s [domain:abilities/deadlock-gravnet-detonation-flash]. Slots are player-confirmed [domain:abilities/deadlock-slots]. Predictions P1-P9 are logged under `audio-channel-control`.

**Combat report accepted on the corpus (2026-09-24).** All 20 sessions with HUD reads are scanned (`killfeed-portrait-0.3.0`, `combat-report-0.4.0`), and the per-round verdict equals `checks.KNOWN_KD` on all 17 that have one, e.g. [metric:combat_report/acceptance@043bafca271a#kills_verdict=11] and [metric:combat_report/acceptance@c62c2b06bcfb#deaths_verdict=15]. Recorded per session as `combat_report/acceptance` (`prototypes/combat_report.py acceptance`). Every report/killfeed disagreement was read in frames and all are killfeed errors: deaths it never saw, an ability kill, a kill of an enemy on a second life, a Me -> Me self entry, a split track, a post-round kill where rounds touch. Frames also fixed three report rules (`combat-report-round-0.8.0`): a team kill (KILLED on an ALLY row) is not a kill; the death flash can delay the killfeed 2.5 s past the panel; an early panel that does not read the previous round's frozen death panel is its own round's death. The player confirmed `5822b6646448` round 16: a Run It Back death then a real death by the same enemy, one report row summing both [domain:rounds/run-it-back-in-report]; the killfeed never marked the real one. ALLY rows are read by the word in the weapon slot (13 corpus-wide, none with a hit) and bounded by the ally lineup; none is named yet. Rows named: [metric:combat_report/acceptance@a06f04a0059f#rows_named=49] of 50 there, 539 of 721 corpus-wide with 16 identity disagreements, 13 of them involving the scoreboard bound-of-one channel -- read those next. `reticle deaths` stores death verdicts keyed by entry onset and slot, and report rows bind to them (every KILLED YOU row on the two labelled sessions). `status` shows the verdict through `round_verdicts`; `coach` (0.2.0) attaches it and counts Run It Back as `player_second_life`. Weapons: the player's magazine | reserve table is [domain:weapons/magazine-and-reserve] (three pairs shared); the HUD ammo counter reads at 304 of 307 kills and is the only stored witness of the player's gun. Decomposition [domain:combat_report/damage-decomposition] stays in the prototype until a capacity reader exists.
