# NOTES archive through 2026-09-25

Paragraphs retired from `NOTES.md` on 2026-09-25, verbatim.

**Weapons named from the killfeed; revives are not deaths (2026-09-24).** `reticle scan` stores each killfeed entry's weapon-slot descriptor (`killfeed_weapon`); `reticle deaths` names it through `adjudication.weapon` and `weapon-gallery-0.2.0`, built by `prototypes/weapon_icons.py gallery` from the player's group names (`labels/weapon_icon/`) and per-entry names (`labels/killfeed_icon/`): a gallery of the player's names. 0.2.0 splits the "Ability" group into Aftershock, Orbital Strike, Boom Bot, Not Dead Yet and Resurrection; left one session out it named [metric:weapon_icons/entries-loso@corpus#not_dead_yet_right=12] of 16 Not Dead Yet entries, none wrongly. `death-adjudication-0.9.0` stores an entry whose icon is Not Dead Yet or Resurrection as a revive (`is_revive`) [domain:killfeed/revive-entries]: 22 corpus-wide, two unlabelled ones confirmed in frames, and the scoreboard witness refuses an interval that holds a revive (a revived Skye died again at `b3b9defb6fd7` 1308.0 s). The combat report equals `KNOWN_KD` on all 8 revive sessions. **The weapon-slot locator (hud-0.13.0, killfeed-weapon-0.2.0)** prefers line art, finds ringed ult icons and the Blade Storm knife [domain:killfeed/jett-blade-storm-icon], and ignores the killstreak numeral [domain:killfeed/killstreak-indicator]: [metric:weapon_slot_locator/corpus-cache@corpus#box_moved=431] boxes moved, K/D unchanged on 20 sessions, 32 identity verdicts moved, none to another name. The player then labelled the knives: `weapon-gallery-0.3.0` names Blade Storm (17 of 17 left out by session), weapons resolved [metric:weapon_reader/accept@corpus#weapon_resolved=2894], revives 34, and the hexagon on a self entry is an environmental death [domain:killfeed/environmental-self-entry]. `death-adjudication-0.10.0` publishes each ability icon's caster as a killer claim (`adjudication.weapon.caster_claim`) and names icons only from abilities the lineup can cast: resolved killers [metric:ability_killer_claims/corpus@corpus#killers_resolved_after=1730] (1660 before); all four icon/portrait disagreements are portrait errors. 0.11.0 lets an icon-named killer label portrait exemplars: [metric:icon_labelled_exemplars/corpus@corpus#names_swapped=10] names swapped, all corrections, 184 gained, 66 withdrawn (8 of 14 sampled were wrong). `reticle reliability` scores each identity channel per agent (Beta beliefs, agents shrunk toward their channel) against independent witnesses, the player's labels of 120 portrait-only names (`prototypes/label_feed_portraits.py`: [metric:identity_reliability/corpus@corpus#portrait_label_right=116] right, 2 wrong) and the held-out icon labels (39/39); `reticle deaths` stores each name's `p_named`. Killer portrait crops sit right of the face when the name's first run is missed (player). Next: the assist panel, reusing these beliefs.
