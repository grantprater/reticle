# Round 4 scoreboard binding attempt — 2026-09-23

**Outcome: blocked at the first identity binding.** The frozen `a06f04a0059f`
round remains 232000–351000 ms. The contract's `--minimum-named 3` real command
fails with “only 2 named death(s); need 3.” The same command at the existing
two-name baseline emits seven deaths: Reyna and Phoenix named, five explicit
identity refusals, and seven location refusals. Its verdicts and summary are
identical to `teststore/death-round4-refusals/events.json`.

Before changing a reader, I checked the stored baseline: eight scoreboard
openings and 80 ten-row observations in this round. The closest opening after
the 281500 ms ally death is 282500 ms. The row observations carry
`scoreboard-0.1.0`, K/D/A and portrait descriptors, but no player or agent name.
The victim portrait is `a06f04a0059f:16890:0:victim` at
`killfeed-portrait-0.2.0`. I inspected the existing death source composites and
the newly annotated 282500 ms scoreboard crop, then logged a falsifiable
cross-surface matching prediction in the store's `notes/predictions.jsonl`.

That prediction failed. Euclidean distances between the stored victim colour
descriptor and all ten scoreboard row descriptors put enemy row 8 first at
0.3405, even though the death is on the ally side. Among ally rows, rows 3, 4,
and 2 score 0.3483, 0.3534, and 0.3565; the visible matching player is row 1,
which scores 0.4622. The descriptor therefore gives neither a valid side match
nor a unique ally match. This is a cross-surface failure, not a margin to tune.
The source displays the victim player name beside the killfeed portrait and on
scoreboard row 1, but neither name is stored by a reader. K/D changes alone
cannot supply the missing identity. No human name, refused lineup guess, or
descriptor ranking was promoted.

**Player follow-up.** The player selected the second displayed row (zero-based
row 1) and observed that it is dimmed, while explicitly expressing uncertainty
about whether dimming always means dead. The answer is retained separately in
`teststore/death-round4-scoreboard/player-review.json` as review evidence. In
the source, the same player row is bright at 262500 ms, dim at 282500 ms, and
has moved to displayed row 2 by 307500 ms. At 282500 ms its stored
`portrait_detail` is 45.6 versus 86.8–110.0 for the other ally rows; at 262500
ms it was 59.1. This supports a dimming hypothesis in the inspected openings,
but transparency and row reordering prevent treating that raw number or ordinal
as a general dead-state and identity reader.

I also tested the bright 262500 ms portrait as a cross-surface match to the
281500 ms victim, after logging a second prediction. Enemy row 9 remains
nearest overall (distance 0.6261). The reviewed ally row 1 is nearest on its
side (0.6481), only 0.0197 ahead of ally row 2. There is no validated gate
that would make that rank an independent death-to-row or agent binding. Both
follow-up predictions and measured outcomes are in the store prediction log;
the machine result remains null.

## Ordering follow-up

The player suggested that rows may be ordered by kills, then assists, then
fewest deaths. The inspected August source openings at 262500, 282500, and
307500 ms are consistent with kills and assists affecting order. At 282500 ms,
WhaleKicker and Me each show three kills, with WhaleKicker above Me and three
assists to Me's zero. At 307500 ms, Me shows four kills and has moved above
WhaleKicker, who shows three. The source does not isolate a deaths tie-break,
and these examples do not establish an exact sort rule. Riot's
[13.06 patch notes](https://playvalorant.com/en-gb/news/game-updates/valorant-patch-notes-13-06/)
say scoreboard position is now determined by Performance Score; the source VOD
predates that September 2026 change. A fixed K/A/D tuple should not be encoded
as a game invariant across versions.

The stored reader emits `display_row` as a screen position. Credit
reconciliation groups that position only inside one opening; death attribution
does not use it to track a player across openings. The reviewed row movement
shows why an ordinal cannot stand for player identity. An ordering constraint
could prune *candidates* after independent row identity and reliable K/D/A
reads exist, with reorderings and ties left unresolved. The current round has
only **4 complete K/D/A rows out of 80**. Its apparent stored kill inversion at
282500 ms is an OCR error against source. A repeated apparent inversion at
`043bafca271a` 885500 and 888000 ms was also an OCR error on the inspected
source frames. Neither is evidence for a different sort rule. The ordering
prediction and these source checks were recorded without promoting an identity.

The [stored comparison and player question](../teststore/death-round4-scoreboard/question.html)
are generated by `tools/death_scoreboard_question.py`. The JSON alongside the
question retains all ten row keys and distances, both producer versions, and a
null binding. A player answer is review truth only. The bounded next step is a
reader that stores a checkable player-name or portrait-to-agent observation and
a separately justified link from that observation to the death. Until then the
five refusals stay null.

I reviewed all seven approved source composites again against the new events.
The fixed reviewed set contains the same seven entries at 281500, 283500,
284500, 295000, 295500, 301000, and 332500 ms, with zero extra or wrong
machine names in that set. The player-reviewed victims remain Deadlock, Reyna,
Jett, Miks, Skye, Phoenix, and Iso respectively; those answers were not machine
inputs. The intervening video was not exhaustively reviewed.

Task record: blocked; all seven contract reading routes were selected and the
relevant sections inspected, plus the working map and identity arbiter. Measured
machine correction: zero. The real output and all seven approved source frames
were reviewed. The focused 25 tests passed; pickup `doctor` showed 11 findings
and zero errors. Development time and token use were not measured.

## Resolution — scoreboard agent and dimming witness

**Outcome: passed.** The real contract command with `--minimum-named 3` emits
seven deaths with four correct machine names (Deadlock, Reyna, Miks, Phoenix),
three enemy identity refusals, and seven location refusals.

The first failure was the portrait box, not the descriptor. Source crops of all
eight openings show the agent portrait as the square cell at `table_x0`, one
row high. The stored `portrait_x0` lay 9–41 px right of it and moved between
openings on one table, so the colour descriptor mostly measured the slab. That
explains the enemy-first ranking above.

`scoreboard-0.2.0` places the box at the table edge and scores each portrait
against the 29 official `agent_icon` drawings. It uses masked normalised
correlation over a small shift and scale search, and stores the raw best
agent, runner-up, margin and gain. The gain is the least-squares slope of row
pixels on art pixels. The reader names nothing. `adjudication/scoreboard.py`
(`scoreboard-agent-0.1.0`) accepts an opening only when all ten rows pass the
agent gate (score ≥ 0.75, margin ≥ 0.25), every gain falls outside 0.60–0.75,
and each side names five distinct agents. Both predictions were logged before
measurement and confirmed:

- Five expanded openings (262500–319000 ms) name ally Breach, Deadlock,
  Phoenix, Reyna and Miks, and enemy Jett, Killjoy, Skye, Iso and Omen. Each
  player keeps the same agent through reordering.
- The game dims a dead player's portrait rather than removing it
  [domain:rounds/scoreboard-dead-dimmed]. Live gain was 0.84–0.95 and dimmed
  gain 0.30–0.41. At every accepted opening the dimmed set per side equals
  the killfeed deaths before it.
- At 237500 ms the board is still fading in and four live allies read dim.
  The whole-board gate refuses that opening because its enemy rows fail the
  agent gate. 238000 and 337000 are refused as well.

The `scoreboard_dim` witness in `adjudication/death.py` compares the accepted
openings on either side of each death. It names 281500 Deadlock (one ally
death, one newly dimmed agent). It names 295000 Miks by elimination: three
ally deaths, newly dimmed {Miks, Phoenix, Reyna}, with the other two deaths
already named Reyna and Phoenix by the killfeed portrait in a first pass
that excludes this witness. Enemy deaths 284500 and 295500 share the
interval with newly dimmed {Jett, Skye} and no independent order, so they
refuse as `interval_unordered`. 332500 has no accepted opening after it.
Deadlock and Miks agree with the killfeed portrait's refused best guesses,
which were not inputs.

Source review: all seven approved composites were rechecked. Entry 1's victim
is WhaleKicker with the Deadlock portrait, and entry 4's is tin with the Miks
portrait. Both match the scoreboard rows and the player's earlier answers.
There are zero wrong names and zero extras in the reviewed set. The player
answers were checks only.

**Lineup disagreement found, not promoted.** The stored lineup accepts Clove
on the enemy side, where the board shows no Clove, and refuses a Raze slot that
the board shows as Breach on the ally side. *Correction, 2026-09-23:* this
paragraph first attributed the Clove to Killjoy's slot. The source top bar
shows it held Jett's slot; see [the scoreboard lineup](SCOREBOARD_LINEUP.md).

The earlier two-name baseline `events.json` and `review.html` in
`teststore/death-round4-scoreboard/` were byte-identical to
`teststore/death-round4-refusals/`. They were renamed `events-baseline.json`
and `review-baseline.html`. The session's scoreboard events were rescanned
under 0.2.0. Full suite: 596 tests pass; `doctor` 11 findings, zero errors.
