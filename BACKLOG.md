# Reticle task queue

`docs/tasks.json` defines executable checks. This file orders work and records why a task is active. Historical arguments and completed items are in [the dated backlog archive](docs/archive/BACKLOG-through-2026-09-23.md).

## Agreed order (2026-09-23)

Steps 1 to 5 are done; steps 3 to 5 are recorded under Completed. Step 1 measured scoreboard availability from stored rows: 33 of 52 sessions have no scoreboard rows, and in the 17 with rows and rounds, 326 of 348 rounds have at least two separate Tab holds. Usable openings around each death remain unmeasured outside `a06f04a0059f`.

6. **Minimap identity, ally slice first.** First slice built and measured ([results](docs/ALLY_MINIMAP_IDENTITY.md)): `scan --only ally_icon` stores a descriptor per ally icon at 2 Hz (`ally-icon-0.1.0`), refusing spawn barriers by their interior's match to the baked map, and `identity.claims_from_ally_icons` names each frame's icons with `assign_side` over the board's ally set minus the player. On `a06f04a0059f` it names 63% of described icons; the source check found 2 wrong of 40, one over the predicted limit, both on overlapping icons. `reticle lifetimes` now runs `round_lifetimes` over stored 15 Hz ally icons ([results](docs/ROUND_ENTITIES.md)); its open list (lobe-centred fits, missed icons, merged state, start/end events, names per segment) is the order the player agreed on 2026-09-23 and comes before (a)-(c). Then: (a) ask the player to label a sample of overlapping and non-overlapping icons (`labelling-pass` skill), then measure an overlap refusal against it; (b) session exemplars from named ally deaths, with `depends_on`; (c) a track key so claims accumulate per teammate. Enemy icons are a later, separate promotion from `prototypes/minimap_portrait.py`, whose 93% leave-one-out was partly scored against provisional labels; read `prototypes/CLAUDE.md` first.

The player answered the dimming question: dimmed means currently dead, a Sage revive lights the row again, and Run It Back is not expected to dim [domain:rounds/scoreboard-dim-is-dead]. Clove before Not Dead Yet and a downed KAY/O remain unknown; capture one of each and ask when it occurs.

**Priority: ideate and experiment on the adjudicator as a statistical model (player, 2026-09-23).** Icon position, facing and cone geometry are not measured precisely enough to explain marginal observations such as cone slivers exactly. The adjudicator's job is to ask, cheaply, whether a plausible measurement error explains another observation: if the self facing were off by a few degrees, or the position by a pixel or two, would the cone then cover this sliver? Carry each measurement with its uncertainty, and make the causal, temporal and interpolation rules (track facing, lifecycle, carried positions) likelihoods over those uncertainties rather than hard gates. The player prescribes no technique. Start with ideation: candidate formulations, what each needs stored, and a falsifiable first experiment scored on the grouping labels and the three slivers. Keep refusals as refusals when no plausible error explains an observation. Map geometry is an uncertain input too: a wall edge off by a pixel explains a sliver as well as a facing error does. Slivers are also evidence for iteratively improving the geometry, the lit-region reader and the icon detectors. Geometry refinements pool evidence across sessions into the geometry builder under a new stamp, never a per-session correction, and any refinement is scored on held-out slivers or an independent witness, because tuning on the observations the model already explained grades its own homework.

**Tiered verification (player, 2026-09-23).** Add a middle tier between unit tests and corpus runs: a fixed handful of sessions with known answers, run in minutes, as the default sanity check. Start with the Omen demo `e78e75b2d191` and a bounded slice of `a06f04a0059f`. A 30-minute corpus run is for acceptance, not for sanity checks.

**Store the team's adjudicated vision, then feed it to ability refusal (next, 2026-09-23).** The chain the player built -- `cone.resolve_lobe` per frame, `track` resolved facing, `minimap_lifecycle` eligibility, `cone.observable` union (`adjudicated_agg`) -- runs only inside `overlay` and stores nothing, so no adjudicator can consume it. Make it a stored product with its own owner entry and stamp, reached by `scan` or a stored-data command, and delete its items from `uncalled_debt.toml`. Then `adjudication.ability.light_refusals` consumes it for the three cone slivers `lit_mask` misses, without restating any stage; score against the grouping labels (16 of 19 viewcones refused today, 0 of 31 abilities). Decide the order of adjudicators explicitly: a smoke blocks the drawn light while the light refuses ability candidates, so that loop needs breaking by time (the previous frame's accepted entities) or a bounded fixed point over stored data.

**Smoke attribution (next for smokes, 2026-09-24).** `adjudication.smokes` finds the team's smokes but names nobody. Apply the player's rules [domain:abilities/smoke-attribution] -- a lone team smoke agent, the player's own tray, or the disc lifetime per agent, refusing where two agents' lifetimes are within error -- as identity claims through `adjudication.identity`, never a local name. Simultaneous births are one bulk cast [domain:abilities/smoke-bulk-confirm]. Measure the minimap disc lifetime per smoke agent first (Miks 18.2 s [domain:abilities/miks-smoke-minimap-disc], Omen's Dark Cover 15 s solo), then test on a session with two team smoke agents. Enemy smokes show only as missing cone light [domain:minimap/enemy-smokes-block-cones] and wait on the stored team vision above. Placement and timing statistics belong to the analysis and coaching layer.

**Minimap widget defects (2026-09-24):** the settings menu's dim overlay passes `minimap.widget_drawn` (`6bb88dba5d2c` at 44.0 s), and `radar_circular_mask` centres an ROI-inscribed circle while the drawn ring circumscribes the map [domain:minimap/widget-ring], clipping map pixels on 6 of 12 geometries.

**Roster defect:** `roster.resolve` reads 1 on a wiped side ([details](docs/BOARD_ALIVE_SETS.md#results)).

**Geometry stamp reads line endings:** `minimap_geometry.source_stamp` hashes the file's raw bytes, so a CRLF checkout (any fresh worktree here, `core.autocrlf=true`) reports all 12 geometry npz stale. Normalising the bytes edits the stamped file, which itself forces a rebuild; schedule it with the next geometry rebuild.

**Killer crop with assist icons:** at 284500 in `a06f04a0059f` icons left of the killer portrait likely corrupt its descriptor ([details](docs/IDENTITY_EXEMPLAR_LOOP.md#open)); `killfeed` owns the crop.

**Combat report acceptance and the killfeed error list (next, 2026-09-24).** `reticle combat-report` stores per-round kill/death/assist verdicts (the report where shown, the killfeed otherwise) and names report rows through `adjudication.identity` (killfeed portraits of the flagged entries, kept inside the scoreboard K/D bound, spread by portrait cluster): [metric:combat_report/identity@a06f04a0059f#right=49] of the player's 50 labels right, none wrong. Next: scan the corpus with `--only hud` (portraits) and `--only combat_report`, then run `reticle combat-report` everywhere for acceptance, and label one more session with `prototypes/label_report_portraits.py`. Killfeed errors the report exposed and not yet fixed: one entry split into two tracks across a detection gap or a slot move (two extra kills on `a06f04a0059f`), and the killfeed killer portrait reading Iso on four KILLED YOU rows (the assist-icon killer crop defect is a candidate). Outgoing rows still lack the player's own weapon. Bind report rows to `adjudication.death` entities once deaths are stored.

**Remaining identity drift to retire:** `Lineup.player` still combines the tray, self icon and top bar itself before publishing claims (the `player-agent` exit).

## Completed

- **`combat-report-identity` (2026-09-24):** report rows are named through the identity arbiter from killfeed portraits within the scoreboard bound, by portrait cluster; a labeller (`prototypes/label_report_portraits.py`) scored it on the player's labels.
- **`combat-report` (2026-09-24):** `reticle scan --only combat_report` stores the panel's header score, damage, hit splits and flag-word scores at 1 Hz with no labels; `reticle combat-report` groups panels, assigns rounds and counts kills, deaths and assists from storage. Both sessions reproduce the known K/D: [metric:combat_report/flags@a06f04a0059f#deaths=19], [metric:combat_report/flags@3694746e4e54#kills=14]. Promoted from `prototypes/combat_report.py`.
- **`minimap-smoke` (2026-09-24):** `reticle scan` stores grey-dark and icon-occluded minimap floor at 4 Hz (`minimap_dark`) and `reticle smokes` recomputes smoke tracks from storage with censored onsets and ends. On `a06f04a0059f` the match scans in 301 s; all [metric:smokes/match@a06f04a0059f#both_ends_observed=22] tracks with both ends observed read 17.75-18.0 s, the Miks disc. Promoted from `prototypes/entity_mining_rounds.py`.
- **`identity-exemplar-loop` (2026-09-23):** killfeed portraits also score against this session's portraits labelled by the scoreboard or the player HUD, only where the official art refuses, with `depends_on` on the labelling death. Across 24 rounds of `a06f04a0059f` it adds 32 names (12 of 12 source-checked correct), zero disagreements. [Results](docs/IDENTITY_EXEMPLAR_LOOP.md).
- **`death-killers` (2026-09-23):** the killer is now entity `<death_id>:killer`, named by `adjudication.identity` from the player HUD and the stored killer portraits over the board-constrained side. Round 4 names 5 of 7 killers, all matching source; two refuse on a thin margin and a single view. [Results](docs/DEATH_KILLERS.md).

## Deferred

- **Killfeed HUD work:** Revisit the double analysis per frame when a profile shows that cost or an entry-key failure in `death-portraits`. [History](docs/archive/BACKLOG-through-2026-09-23.md#the-hud-pass-analyses-the-killfeed-twice-per-frame).
- **Remaining minimap, audio, geometry, and coaching work:** Select a bounded task after the active task closes or identifies an upstream blocker. [Historical queue](docs/archive/BACKLOG-through-2026-09-23.md) and [pipeline gates](docs/PIPELINE_REVIEW.md) retain the detail.
